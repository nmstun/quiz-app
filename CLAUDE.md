# 咲登クイズ(四則演算クイズアプリ)

音声入力で答える算数クイズ。個人開発メモの「クイズアプリ」から着手した最初のプロジェクト。

## 概要

- カテゴリ(算数/漢字/都道府県/地図記号/なぞなぞ/モビルスーツ/からぴち/スマブラ/究極の選択/達人)と人数(ひとり/ふたりで交互)と問題数コース(みじかい5問/ふつう10問/ながい20問、究極の選択のみ「えんえん」も)を選んで開始
- 算数: 四則演算(+ - × ÷)のランダムな問題を出題。学年別の出題範囲(1〜2年生/3年生/4年生)を選択可能
- 漢字: 小学校で習う漢字1026字(教育漢字、学年別漢字配当表)から出題。読み方問題と画数問題を約7:3で混ぜて出題。学年帯別の出題範囲(1〜2年生/3〜4年生/5〜6年生)を選択可能
- 都道府県: 47都道府県から出題。県庁所在地問題と、何地方に属するか問う問題を約7:3で混ぜて出題
- 地図記号: 国土地理院「地図記号一覧」を参考に作成したSVGアイコン30種(市役所・郵便局・神社・田・温泉など)を表示し、名称を答える
- なぞなぞ: 固定の問題バンク(100問)からランダムに出題(バンクを使い切ったらシャッフルして再利用)
- モビルスーツ: 機体の特徴を短く説明する文から名前を四択で答える「機体名問題」と、機体名から型式番号(型番)を四択で答える「型番問題」を約7:3で混ぜて出題(75機)。著作権に配慮し、画像・シルエットは使わずテキストのみで出題
- からぴち: ゲーム実況グループ「カラフルピーチ」から四択で出題(122問)。メンバー12人のメンバーカラー・誕生日・星座・血液型・好きなこと・苦手なことを、名前→値/値→名前の双方向で問う。カラーは色そのものを丸で表示する視覚問題もあり。グループや映画についての問題も混ぜる
- スマブラ: 『大乱闘スマッシュブラザーズ SPECIAL』のファイター84体について、出典ゲームシリーズを四択で出題(117問)。ファイター→シリーズ、シリーズ→ファイターの双方向とゲームのトリビア
- 究極の選択: **正解の無いパーティークイズ**。「どっちをえらぶ?」の2択を1問ずつその場で組み立てて出題する。採点せず、選んだものを記録して結果画面で振り返るだけ(スコア・ベスト記録なし)
  - このカテゴリだけ「えんえん(おわりなし)」コースを選べる。盛り上がっている最中に問題数で打ち切られないようにするためで、「おわる」を押すまで続き、押した時点で選んだ分が結果に並ぶ
- 達人: 算数・漢字・都道府県・地図記号・なぞなぞ・モビルスーツ・からぴち・スマブラを設問ごとにランダムに混ぜて出題(採点しないカテゴリは混ざらない)
- 回答は音声入力(Web Speech API)、非対応環境ではテキスト入力にフォールバック。マイクが使えないときは、原因に応じた日本語の案内を出す(エラーコードは画面に出さない)。モビルスーツ・からぴち・スマブラ・究極の選択は選択ボタンでの回答形式(究極の選択のみ2択、他は四択)
- 終了後に正解数・所要時間・各問題の結果一覧を表示、結果画面から別のコースを選び直すことも可能
- 間違えた問題があれば、結果画面から**まちがえた問題だけをもう一度**解き直せる(出題されるのは同じ設問そのもの)
- コースごとのベストスコア(正解数優先、同数なら所要時間が短い方が上位)をlocalStorageに記録
- コースごとに直近10回の成績も記録し、結果画面で棒グラフと平均点として振り返れる(2回目以降に表示)
- コース選択画面から記録(ベストスコアと成績の履歴)をクリア可能(確認ダイアログあり)
- 正解/不正解でそれぞれ異なる効果音を再生(Web Audio APIで生成、音声ファイル不要)
- コース選択画面で効果音のオン/オフを切り替え可能(localStorageに保存)
- ふたりで交互モード: 1問ずつ手番を回す対戦形式。ひとり当たりコースの問題数ずつ出すので、セット全体は倍になる(みじかいなら5問ずつ=全10問)。結果は「3 - 2」と勝敗で表示し、ベストスコアは記録しない(ひとりで解いた記録と混ざらないようにするため)
- クイズ中はいつでも「やめる」でコース選択に戻れる(えんえんコースでは「おわる」になり、1問以上答えていればそのまま結果画面へ)
- ライト/ダークの両テーマに対応(`prefers-color-scheme`)
- ビルド不要。`index.html` をブラウザで開くだけで動く静的Webアプリ

## ファイル構成

```
quiz-app/
├── index.html   構造のみ
├── style.css    デザイン
├── data.js      問題データ(script.js より先に読み込む)
├── script.js    ロジック(問題生成・音声認識・採点)
├── test.js      回帰テスト(`node test.js`。依存なし)
├── sw.js        Service Worker(オフライン対応)
├── favicon.svg           アプリアイコン(暖色地に白の「?」)。実体はこの1ファイル
├── favicon.ico           Safari用。以下すべてfavicon.svgから書き出したもの
├── favicon-32.png        32px
├── apple-touch-icon.png  180px
├── icon-192.png          192px
├── icon-512.png          512px
├── manifest.webmanifest  ホーム画面追加時の名称とアイコン
├── scripts/              favicon.ico生成スクリプト
└── CLAUDE.md    このファイル
```

**Safariは `rel="icon"` のSVGを使わない**ため、SVGだけ置くとSafariのタブが空になる。
icoとPNGを併せて置き、`index.html` で ico → PNG → SVG の順に宣言している。
絵柄を変えたら以下で全部作り直す。

```sh
rsvg-convert -w 180 -h 180 favicon.svg -o apple-touch-icon.png
rsvg-convert -w  32 -h  32 favicon.svg -o favicon-32.png
rsvg-convert -w 192 -h 192 favicon.svg -o icon-192.png
rsvg-convert -w 512 -h 512 favicon.svg -o icon-512.png
node scripts/make-favicon-ico.mjs favicon.svg favicon.ico
```

`scripts/make-favicon-ico.mjs` はicoの中身にBMPではなくPNGをそのまま詰めており、
そのPNGはRGBAにしてある(`rsvg-convert` は全ピクセルが不透明だとRGBで書き出すが、
RGBのPNGを入れたicoを受け付けないデコーダーがあるため)。

`index.html` のファビコンの `link` タグには `?v=2` を付けている。ファビコンはブラウザが
通常のHTTPキャッシュ制御とは別の仕組みで保持しており、`Cache-Control: must-revalidate`
を返してもリロードでは差し替わらない。URLが変わらないと古い絵柄が出続けるため、
**絵柄を変えたらこの番号も上げること**。

データとロジックは別ファイルに分けている。データはロジックの3倍近い分量があり、
更新の理由も頻度も違うため。`data.js` は `QUIZ_DATA` という1つのオブジェクトだけを
公開し、`script.js` の冒頭で分割代入して受け取る。**`index.html` では `data.js` を
`script.js` より先に読み込む必要がある**。

## PWA(ホーム画面追加・オフライン対応)

`manifest.webmanifest` と `sw.js` で、ホーム画面に追加すると全画面のアプリとして起動し、
電波が無くても遊べる。

- **iOSは manifest の `display` を見ない**。全画面で開かせるには `index.html` の
  `apple-mobile-web-app-capable` が要る(manifest側の指定だけでは効かない)
- `sw.js` の `APP_VERSION` は **script.js の `APP_VERSION` と同じ値に揃えること**。
  キャッシュ名にこの値を使っており、値が変わることでブラウザが更新を検知して
  古いキャッシュを捨てる。揃え忘れると、更新したのに古いままの端末が出る
- HTMLはネットワーク優先、CSS/JS/画像はキャッシュ優先。HTMLまでキャッシュ優先にすると
  更新しても古い画面が出続けるため分けている
- 動作確認は「`python3 -m http.server` を起動 → 開く → **サーバーを止めて** リロード」。
  それでも起動して出題できればオフライン対応は効いている

## テスト

`node test.js` で回帰テストが走る。ブラウザを使わず、`index.html`と同じ構造のDOMスタブ上で
`script.js`を実行して検証する方式なので、依存パッケージもビルドも不要。
**`index.html`の要素ID/クラスを変更したら`test.js`のスタブも合わせて直すこと**(スタブが
実DOMとずれるとテストが通っても実際には壊れている、という状態になり得る)。
UIを目視確認したいときはリポジトリ直下で `python3 -m http.server` を起動してブラウザで開く
(`file://`だとブラウザがCSS/JSを強くキャッシュして変更が反映されないことがある)。
なお一度アクセスするとService Workerがファイルをキャッシュするので、変更が反映されない
ときはSWの登録解除とキャッシュ削除が要る。

テストを書くときの注意:

- **誤答を作るのに「正解＋余計な文字」は使えない。** 数値の設問は文中の数字が拾われ、
  3文字以上の記述式は部分一致の保険に当たって、どちらも正解になってしまう。
  `answerCurrent()`では数値は別の数、記述式は`'zzq'`を使っている
- **ランダム出題を対象にしたループは、狙った条件が0件のまま終わらないようにする。**
  例えば漢字の読みは8割以上が2文字以下なので、「短い答え」の検証だけで打ち切ると
  「長い答え」の検証が0件のまま通ってしまう。必要な条件が全部そろうまで回すこと

## 主要ロジック(script.js)

- `CATEGORIES` — **カテゴリ定義はこの配列1か所に集約している**。`{ id, label, bank, generate }`
  で、`bank` は出題プールを返す関数(漢字は学年で中身が変わるので関数にしてある)。算数だけは
  決まったプールを持たず毎回その場で作るため `bank` を持たない。`generateSet()`・カテゴリ名の
  表示・達人コースの抽選はすべてこの配列から導出されるので、**カテゴリを増やすときに触るのは
  この配列と `index.html` のボタンだけ**
- `choiceQuestion(text, answer, distractorPool, extra)` — 四択の設問を組み立てる共通処理。
  誤答は候補から正解と同じ値を除き、重複も潰してから3つ選ぶ(星座・血液型・出典シリーズのように
  値が重複するデータでも同じ選択肢が並ばない)
- `countValues(items, pick)` — 値ごとの出現数。「値→名前」の逆引きを出題してよいか
  (その値を持つ項目が1つだけか)の判定に使う


### データ定義(data.js)

- `currentType` — `CATEGORIES`のid、または`'master'`(達人)。カテゴリボタン(`#categoryRow`内`.segmented-btn`の`data-type`)で切り替わる。`master`(達人)は設問ごとにカテゴリを抽選して混ぜる特殊コース
- `KANJI_DATA` — 教育漢字1026字のデータ。各要素は`[漢字, 学年, 画数, [読み方の候補...]]`。出典はKANJIDIC2(Electronic Dictionary Research and Development Group、CC BY-SA)で、学年別漢字配当表の現行版(2020年施行、1026字)と字数が一致することを確認済み
  - 読み方の候補は音訓をすべて含む(判定をゆるくするため)。正解表示には先頭3つまでを「・」で並べる(`answerLabel`)。先頭の1つだけだと、聞き慣れない読みが「正解」として出てくることがあるため
  - KANJIDIC2には読みとして単位表記(志→シリング、粉→デシメートル)が入っていたので除いてある。読み候補がすべてひらがなであることは`test.js`で検証している
- `PREF_DATA` / `REGIONS` — 47都道府県のデータ。各要素は`[都道府県名, 県庁所在地, 県庁所在地のひらがな読み, 地方キー]`。県庁所在地は four4to6/pref_lat_lon(MIT License)のデータを、地方区分(北海道/東北/関東/中部/近畿/中国/四国/九州)は標準的な学校教育の区分を参照して作成
- `MAP_SYMBOLS` — 地図記号30種のデータ。各要素は`{name, svg}`。`svg`は国土地理院「地図記号一覧」ページの公式アイコン画像を目視で参照し、独自に描き起こしたSVGパス(公式画像そのものの複製ではない)
- `MS_DATA` — モビルスーツクイズの問題バンク(75件)。各要素は`{q: 説明文, a: 正解の機体名, code: 型式番号}`。説明文は機体の一般的な特徴を独自に要約したもので、公式のプロフィール文や画像・シルエットは使用していない。`code`はRX-78-2のような公式の型式番号(事実情報)
  - 作品ごとにコメントで区切ってある(一年戦争 / Z・ZZ / 逆襲のシャア・UC / F91・V / G・W・X・∀ / SEED / 00 / 鉄血・水星)
  - 追加時の制約: **機体名と型番はどちらも重複させない**。型番問題は他の項目の`code`を誤答に使うため、重複すると答えが定まらなくなる。説明文も、プール内の他機体と取り違えられない粒度にすること(例: 同じ搭乗者の機体が複数あるときは「最初に搭乗する」「後半に搭乗する」のように書き分ける)
- `KARAPICHI_MEMBERS` — カラフルピーチ(からぴち)12人のデータ。`{name, color, hex, birthday, zodiac, blood, like, weak}`。**実在の人物を扱うため、収録するのは裏取りできた事実だけに限る**
  - `birthday`/`zodiac`/`blood`/`like`/`weak` は公式サイトのメンバーページの記載そのまま(`blood`の「夜型」「まるがた」「AO型」はグループ本人たちによる公式のネタ回答)
  - `color` は公式に記載が無いため、複数のファンサイトで12人分が完全一致することを確認して採用
  - `hex` は色を丸で表示するための近似色。公式の指定値ではない
  - 公式の紹介文をそのまま引用したり、外見を描写したりはしない
- `KARAPICHI_ATTRS` — 属性ごとの出題テンプレート。`forward`(名前→値)と`reverse`(値→名前)を持つ。好きなこと/苦手なことは値が長く選択肢ボタンに4つ並べると読みづらいため`forward`は`null`にしてある
- `KARAPICHI_TRIVIA` — グループ・映画についての問題。`{q, a, d: [誤答3つ]}`
- `KARAPICHI_BANK` — 上記から出題可能な設問をすべて列挙したプール(122問)。**`reverse`はその値を持つメンバーが1人だけのときしか生成しない**(例: じゃぱぱとシヴァは誕生日が同じ12月25日なので「12月25日生まれは誰?」は答えが定まらず出題しない)。加えて、メンバーごとに色を丸で見せる`dir:'swatch'`の設問を持つ
- `DILEMMA_THEMES` / `DILEMMA_PAIRS` — 究極の選択のお題。**固定の問題リストを持たず、1問ずつその場で組み立てる**
  - `DILEMMA_THEMES` … 同じテーマの `items` から2つをランダムに組み合わせる。10テーマ・計507通り。数は稼げるが対比は運まかせ
  - `DILEMMA_PAIRS` … 対比を練った固定ペア18問。「テストの答えが全部わかる魔法」と「一晩で宿題が終わる魔法」のように、組み合わせでは作れない筋の通った2択になる
  - `DILEMMA_PAIR_RATIO`(0.35)の確率で固定ペア、それ以外はテーマの組み合わせ。合計525通り
- `SMASH_FIGHTERS` / `SMASH_TRIVIA` / `SMASH_BANK` — 『スマブラSP』のファイター84体`[名前, 出典シリーズ]`とゲームについての問題。ファイター一覧は複数の攻略サイトで照合済み。ゲーム内の事実情報だけを扱い、キャラクターの画像・デザインは使用しない。ポケモントレーナーとMiiファイターは公式では内訳ごとに数えられることもあるが、出典シリーズを問うクイズでは区別する意味がないため1体にまとめている

### そのほか

- `arithGrade` — `'low'(1〜2年生) | 'mid'(3年生) | 'high'(4年生)`。学年ボタン(`#arithGradeRow`内`.segmented-btn`の`data-grade`)で切り替わる。算数(および算数を含む達人コース)選択時のみ`#arithGradeField`(ラベルを含む行)が表示される
- `ARITH_GRADES` — 学年ごとの演算子セットと数値範囲の定義。学習指導要領の各学年の内容を目安に設定(例: 1〜2年生はわり算なし・九九の範囲、4年生は2〜3桁×2桁のかけ算や2桁の数でわるわり算まで)
- `kanjiGrade` — `'low'(1〜2年生) | 'mid'(3〜4年生) | 'high'(5〜6年生)`。学年ボタン(`#kanjiGradeRow`内`.segmented-btn`の`data-grade`)で切り替わる。漢字(および漢字を含む達人コース)選択時のみ`#kanjiGradeField`(ラベルを含む行)が表示される。`KANJI_GRADE_RANGES`で学年帯→学年の範囲(例: `mid`→3,4)を定義し、`getKanjiBank()`が`KANJI_DATA`をその学年範囲でフィルタする
- `generateSet()` — `currentType`に応じて出題を生成
  - `arith`: `generateArithQuestion()`で`arithGrade`に応じた`ARITH_GRADES`の範囲から毎回ランダム生成。減算は常に非負、除算は常に割り切れる組み合わせのみになるよう制約
  - `riddle`: `RIDDLES`配列(100問、`{q, a: [正解表記の配列]}`)から`sampleFromBank()`でTOTAL問抽出。子ども向けなぞなぞサイトを参考に、単一の短い答えで判定できるものを選んで作成
  - `kanji`: `getKanjiBank()`(選択中の`kanjiGrade`でフィルタ済み)から`sampleFromBank()`でTOTAL件抽出し、`generateKanjiQuestion()`で`STROKE_QUESTION_RATIO`(0.3)の確率で画数問題(`kanji-stroke`)、それ以外は読み方問題(`kanji-reading`)に変換
  - `pref`: `PREF_DATA`から`sampleFromBank()`でTOTAL件抽出し、`generatePrefQuestion()`で`PREF_REGION_QUESTION_RATIO`(0.3)の確率で地方問題(`pref-region`)、それ以外は県庁所在地問題(`pref-capital`)に変換
  - `map`: `MAP_SYMBOLS`から`sampleFromBank()`でTOTAL件抽出し、`generateSymbolQuestion()`でSVGアイコン問題(`symbol`)に変換
  - `ms`: `MS_DATA`から`sampleFromBank()`でTOTAL件抽出し、`generateMsQuestion()`で`MS_CODE_QUESTION_RATIO`(0.3)の確率で型番問題(「〇〇の型番は?」、`code`から四択)、それ以外は機体名問題(説明文→`a`から四択)に変換。どちらも不正解3件はMS_DATAの他の項目からランダムに選出
  - `kara`: `KARAPICHI_BANK`から`sampleFromBank()`でTOTAL件抽出し、`generateKarapichiQuestion()`で四択に変換。誤答は`forward`なら他メンバーの同じ属性の値(星座・血液型は値が重複するので重複を除いてから選ぶ)、`reverse`/`swatch`なら他メンバーの名前
  - `smash`: `SMASH_BANK`から`sampleFromBank()`でTOTAL件抽出し、`generateSmashQuestion()`で四択に変換。**「シリーズ→ファイター」の逆引きは、そのシリーズからの参戦が1体だけのときしか生成しない**(例: スーパーマリオからは9体参戦しているので答えが定まらない)
  - `dilemma`: `generateDilemmaQuestion()`で毎回その場で生成(`bank`を持たない)。`scored: false` を付けて採点対象から外す
  - `master`: 設問ごとに`arith`/`kanji`/`riddle`/`pref`/`map`/`ms`/`kara`/`smash`を`randInt()`で抽選し、`CATEGORIES`の`generate`を呼ぶ。漢字・なぞなぞ・都道府県・地図記号・モビルスーツ・からぴち・スマブラはTOTAL件分を先に`sampleFromBank()`で確保したプールから順に消費するため、1回のセット内で重複しない。漢字は`arithGrade`同様、達人コースでも`kanjiGrade`の範囲が適用される
  - いずれもバンクを使い切ったらシャッフルして継ぎ足す(`sampleFromBank()`)
- `renderQuestion()` — `questions[current]`の内容に応じて表示を切り替える
  - `svg`を持つ設問: `questionEl.innerHTML`にSVG(`.symbol-svg`)と、問題文をキャプションとして描画。図の表示と回答方法は独立していて、地図記号(`symbol`)は記述式、からぴちのカラーは選択式になる
  - `choice`: マイク・テキスト入力(`#controls`)を隠し、`#choiceList`に4つの選択肢ボタンを動的生成。クリックで直接`submitAnswer()`を呼ぶ。モビルスーツ・からぴち・スマブラが共用するため、型名はカテゴリに依らない`choice`にしている
  - それ以外: `textContent`で通常の問題文を表示
  - 問題文のサイズは3段階。算数はクラス無し(最大)、それ以外は18文字以下なら`medium-text`、超えるなら`long-text`。文字数だけだと算数の式(「655 + 396 =」は11文字)と日本語の問い(「「板」の読み方は?」は9文字)を区別できないため、まずタイプで分けている
  - `replayAnimation()`で毎問フェードイン(`q-enter`)、採点時は正解`feedback-correct`/不正解`feedback-wrong`のアニメーションを再生する
- `extractNumber(raw)` — 算数専用。音声認識/テキスト入力の文字列から数値を抽出
  - 半角/全角の算用数字に対応
  - 簡易的な漢数字パーサ(一〜九、十、百の組み合わせ、0〜999程度)にも対応
  - 「マイナス」を負号として解釈
  - ひらがなの読み(「じゅうさん」「さんびゃくよんじゅうご」など)にも対応。`kanaToNumber()`が桁("せん/ひゃく/じゅう")と数字を左から積み上げて数に直す。促音・連濁("ろっぴゃく""さんぜん""はっせん")も辞書に入れてある
  - ひらがなの判定は**定型句を除いた残り全体が数詞として読み切れるときだけ**採用する。部分一致で拾う方式にすると「わかりません」の「ません」が「せん(1000)」に化けるため。前後の「こたえは」「〜です」「〜だとおもいます」等は`answerCandidates()`で落とす
  - カタカナで返す音声認識もあるため、ひらがなに寄せてから解析する
- `answerCandidates(text)` — 「こたえは」「〜です」「〜だとおもいます」といった前置き・語尾を剥がしていき、その途中経過をすべて候補として返す。剥がしすぎて答えそのものを削っても元の形が候補に残るので取りこぼさない(答えが「かな」でも通る)。算数のひらがな数詞と記述式の両方がこれを使う
- `isTextAnswerCorrect(raw, accepted)` — 記述式(なぞなぞ・漢字の読み方・都道府県など)用。記号除去・カタカナ→ひらがな変換・小文字化した上で、`answerCandidates()`の候補と**完全一致**するかで判定する
  - 以前は素朴な部分一致だったが、「くも」のような短い答えだと「ぼくもわからない」のような無関係な発話でも正解になっていた
  - 3文字以上の答えに限り、想定外の言い回しへの保険として部分一致も残している(誤判定より、正解を取りこぼして子どもががっかりする方が痛いため)
- `isChoiceAnswerCorrect(raw, accepted)` — 四択用。**完全一致でしか正解にしない**。部分一致のままだと、正解が「ガンダム」のときに選択肢の「ユニコーンガンダム」を選んでも正解になってしまう(モビルスーツクイズで9通りの誤判定が起きていた)
- `submitAnswer()` — `questions[current].type`が`arith`/`kanji-stroke`(数値)かそれ以外(テキスト)かで分岐して採点し、2秒後に次の問題または結果画面へ遷移。`choice`(選択式)のボタンもクリック時にこの関数を直接呼ぶ
- `setInputsDisabled()` — 連打防止用に送信ボタン・テキスト入力・マイクに加え、`#choiceList`内の選択肢ボタンもまとめて無効化する
- `nextTimer` — 次の設問へ進める`setTimeout`のID。「やめる」で中断したときに`clearTimeout`しないと、コース選択に戻った直後にタイマーが発火してクイズ画面へ戻ってしまうため必ず保持・クリアする
- 四択の採点後は、正解のボタンに`is-correct`、選んだ誤答に`is-wrong`を付けて色で示す(`disabled`より優先させるCSSを併記)。選択式は押したボタン自体が答えなので`#answerDisplay`への復唱は行わない
- `TOTAL` — コース選択(`.course-btn`の`data-count`)で決まる出題数
- 成績の履歴 — `sakitoQuizHistory`に`{コースキー: [{c: 正解数, t: 問題数, ms: 所要時間}]}`の形で保存する
  - ベストスコアは「一番よかった1回」しか残らず、伸びているのかが分からないため別に持つ。コースキーはベストスコアと同じもの(学年込み)を使うので、1つの配列の中では`t`は常に同じ値になる
  - `HISTORY_LIMIT`(10)を超えたら古いものから捨てる。localStorageを無制限に太らせないため
  - 棒の高さは`8 + 正解の割合 × 92`%。全問不正解でも棒が消えないよう下限を持たせている
  - 1回しか記録が無いうちは比べようがないので出さない。ふたりで交互・復習・採点しないカテゴリでは`hideHistory()`で消す(記録も残さない)
- `reviewing` / `reviewQueue` — まちがえた問題だけの復習モードと、その出題内容
  - `updateReviewButton()`が結果表示のたびに`results`と`questions`を突き合わせ、不正解だった設問を`reviewQueue`に控える。1件でもあれば`#reviewBtn`を出し、そのぶん「もう一度挑戦」を`ghost-btn`に下げる(復習の方が優先したい行動のため)
  - `startQuiz(reviewSet)`に設問の配列を渡すと、新しく作らずそれだけを出す。引数なしの通常開始と同じ関数にしているのは、中断・タイマー・画面遷移の後始末を二重に書かないため。**`retryBtn`のハンドラを`startQuiz`にそのまま渡さないこと**(クリックイベントが第1引数に入り、復習モードとして始まってしまう)
  - 復習は間違えた分だけの短いセットなので、ベストスコアは保存も表示もしない
  - ふたりで交互のときは、`reviewQueue`の各設問に間違えた本人の`player`を持たせ、`currentPlayer()`がそれを優先する。復習は本人が答え直さないと意味がないため
- `playerCount` / `setSize` — 人数(`#playerRow`の`data-players`)と、そこから決まるセット全体の設問数(`TOTAL × playerCount`)
  - 手番は`playerOf(i) = i % playerCount`で何問目かから導く。「いま誰の番か」を別の変数で持つと、中断・やり直し時にずれる余地ができるため。実際に参照するのは`currentPlayer()`で、復習モードのときだけ設問に紐づいた`player`を優先する
  - 出題数・進捗ドット・終了判定はすべて`setSize`を見る。`TOTAL`はコース(1人あたりの問題数)を指したままにして、ベストスコアのキーと結果ラベル(「ふたりで5問ずつ」)に使う
  - ふたりのときは`showVersusResult()`に分岐し、ベストスコアの保存も表示もしない
  - `results`の各要素に`player`を持たせ、結果一覧の行頭に「1P」「2P」のタグを出す
- `endless` — 「えんえん」コース(`data-count="0"`)を選んだかどうか。`scored: false`のカテゴリを選んだときだけ`#endlessCourseBtn`が現れる
  - 設問は`ENDLESS_CHUNK`(10)件ずつ作り、`current`が末尾に達したら`scheduleNext()`が継ぎ足す。最初から大量に作らないのは、どこまで遊ぶか分からないため
  - 重複防止の記録`seenKeys`は`generateSet()`で作り直し、継ぎ足しでは引き継ぐ。チャンクごとに作り直すと境目で同じ設問が出てしまう
  - 進捗ドット(`#progress`)は残りが読み取れないので隠し、「n問目」とだけ出す
  - `TOTAL`は結果表示やベストスコアのキーにも使うため、えんえんコースでは書き換えない(次に通常コースを選んだときに壊れないようにするため)
- `scored: false` — **正解が無いカテゴリの印**。カテゴリ定義と、そこから作られる設問の両方に付く。これ1つで
  - `generateSet()`の達人コースで抽選対象から外れる(達人は採点前提のため)
  - `submitAnswer()`が採点せず、選んだものを`results`に`correct: null`で記録する
  - `renderProgress()`の進捗ドットが正誤の色ではなく中立色(`answered`)になる
  - `showResult()`が`showUnscoredResult()`に分岐し、スコアもベスト記録も出さない
- `generateUnique(count, make)` — バンクを持たず1問ずつ作るカテゴリ(算数・究極の選択)向け。バンクから引く場合と違って同じ設問が二度出るため、重複したら作り直す。20回試して駄目なら諦めて採用する
- `showResult()` — `localStorage`のキー`sakitoQuizBestScores`に `${currentType}-${TOTAL}` (例: `riddle-10`) ごとのベストスコアを保存。算数は`arith-${arithGrade}-${TOTAL}`、漢字は`kanji-${kanjiGrade}-${TOTAL}` (例: `arith-high-10`、`kanji-low-5`)として学年ごとに分けて記録する。`isBetterScore()`で正解数→所要時間の順に比較

### 音声認識のエラー処理

- `recognition.onerror`は`'not-allowed'`のような英語のコードで来る。以前はそれをそのまま画面に出しており、実機で拒否したときに「音声認識エラー: not-allowed」と表示されていた。`RECOGNITION_ERROR_MESSAGES`で、次に何をすればいいかが分かる日本語に置き換える
- `'aborted'`だけは無視する。バックグラウンド遷移や回答時にこちらから`abort()`したときに来るもので、ユーザーから見れば異常ではないため
- `'not-allowed'` / `'service-not-allowed'`(=権限が無い)のときは`denyMic()`で**マイクボタン自体を止める**。この状態でタップしても何も起きず、押し続けて詰まってしまうため。あわせて`#note`に戻し方(端末の設定で許可 → ページを読み込み直す)を出す
- `micDenied`は`setInputsDisabled()`でも参照する。設問が変わるたびに`micBtn.disabled = false`に戻ると、押しても反応しないボタンが復活してしまうため
- 一時的なエラー(`'no-speech'`など)ではマイクを止めない。もう一度押せば使えるため

## デザイン方針(style.css)

- 色・角丸・影は`:root`のCSS変数に集約し、`@media (prefers-color-scheme: dark)`で同名の変数を上書きするだけでダークモードが成立するようにしている。個別セレクタに生の色を書かない
- コース選択画面の各コントロールは`.field`(ラベル+本体)でまとめる。カテゴリ・学年・問題数の3行が並ぶため、ラベルが無いとどれを選んでいるのか判別できない
- カテゴリは項目数が3の倍数でないため、`.segmented--wrap`で等分割せず自然幅で折り返す(等分割すると最終行の項目だけ不自然に引き伸ばされる)
- `prefers-reduced-motion: reduce`ですべてのアニメーション・トランジションを実質無効化する

## 動作確認済み事項

- 問題生成を20,000回シミュレーションし、減算の負値・除算の割り切れないケースが0件であることを確認済み
- `extractNumber()` の主要パターン(算用数字/全角数字/漢数字/ひらがな読み/マイナス表現/文中の数字)をNode.jsでユニットテスト済み(36/36 pass)。ひらがなは1〜4桁・促音・連濁・前置きや語尾つきの発話に加え、「わかりません」「せんせい」のような数でない発話を弾くことも確認している
- `test.js`の回帰テストで、全カテゴリ・全学年の出題範囲と採点、連打防止、中断時のタイマー停止、四択の正誤マーキング、効果音のオン/オフ、マイク解放、ベストスコアの保存キー、漢字の読み方の正解表示、ひらがな読みでの解答、音声認識のエラー表示と権限が無いときの挙動、えんえんコースの継ぎ足しと終了、ふたりで交互モードの手番・勝敗・記録非保存、まちがえた問題だけの復習、成績の履歴(上限・コース別・記録しない画面)を検証(224項目 pass)。からぴち(122問)とスマブラ(117問)は出題プールをひととおり出題させ、設問文から正解が一意に引けること・逆引きが答えの定まるものだけであることを確認している。四択が完全一致で採点されること(正解を部分文字列として含む誤答を選んでも不正解になること)も検証している
- ローカルHTTPサーバ(`python3 -m http.server`)経由でライト/ダーク両テーマ、モバイル幅、キーボード表示時のcompactモードを実機同様に目視確認

## 残っている作業

いずれも実機が要るもので、コード上でできることは無い。

- [ ] スマホでの音声入力の権限リクエストUXを確認する(iOSはブラウザとホーム画面追加とで挙動が違う)
  - 実機で確認済み: 初回タップで権限ダイアログが出る / 一度許可すれば次回以降は聞かれずに録音が始まる
  - 実機で判明して対応済み: 拒否したときに`not-allowed`というエラーコードがそのまま画面に出ていた
  - 未確認: ホーム画面に追加したPWAでの権限の扱い(Safariとは別扱いになる)
  - 音声からの数値の読み取りは実機(iOS)で確認済み。ひらがなの読み上げ(「じゅうさん」)と、前置き・語尾つきの発話(「こたえは42です」)がどちらも正しく採点されることを確認した
  - マイクの解放は実機(iOS)で4場面すべて確認済み。「聞き取り中」のままバックグラウンドへ / 回答直後 / **最終問題を答えて結果画面へ移った後** / 「やめる」で中断。いずれもマイク使用中のインジケータが消えることを確認した(3つめは過去に不具合が出た箇所)
- [ ] 音声認識非対応ブラウザ(Firefox、Safariの一部バージョン)での挙動を実機確認する。テキスト入力へのフォールバック自体は実装済み

## 仕様上の割り切り

直すべき不具合ではなく、意図してそうしている点。

- `MAP_SYMBOLS`のSVGは国土地理院の公式画像を参考に独自に描き起こした簡易版で、正式な記号とは細部が異なる。公式画像そのものは著作物なので使わない
- 東京都の「県庁所在地」は都庁がある新宿区としている。市ではないため`市区`どちらの表記でも正解になるようにしてある
- からぴちのメンバーの見た目(アバター/キャラクターデザイン)は著作物なので画像として再現しない。視覚問題はメンバーカラーの色玉のみに留めている
- モビルスーツ・スマブラも同様に、画像やシルエットは使わずテキストの事実だけを扱う
- `KANJI_DATA`の読み候補はKANJIDIC2の音訓すべてで、難読・古い読みも含む。判定をゆるくするためあえて絞っていない(表示側は先頭3つまでに広げて対処済み)
- 映画『魔王と予言の少女』のキャラクター名は、公式での明記が見つからず、独立した2つのファン情報源の一致をもって採用している。誤りが判明したら直すこと

## 対応済み

- [x] ひらがな読み上げ(「じゅうさん」等)の数値変換 → `kanaToNumber()`
- [x] 難易度選択(桁数・演算子の絞り込み) → 算数の学年別出題範囲(`arithGrade`)
- [x] 究極の選択に終わりなしモード → 「えんえん」コース
- [x] 2人で交互に答えるモード → `playerCount`
- [x] まちがえた問題だけの解き直し → 結果画面の「まちがえた問題だけ もう一度」
- [x] スコア履歴(過去n回分の推移)の保存 → 直近10回の棒グラフ
- [x] PWA化(オフライン対応・ホーム画面追加) → `manifest.webmanifest` + `sw.js`
- [x] `isTextAnswerCorrect()`の部分一致で短い正解(例:「くも」)が誤判定される → 定型句を剥がした候補との完全一致に変更
- [x] `KANJI_DATA`の難読な読みが正解表示に出る → 正解表示を先頭3つまでに広げ、KANJIDIC2由来の単位表記(志→シリング、粉→デシメートル)を除去
- [x] `MS_DATA`が21件と少なめ → 75件に増量
- [x] 映画『魔王と予言の少女』のキャラクター名の裏取り → 独立した2つの情報源で一致を確認。裏が取れなかった「スーリヤ王国」「エレナ・イーリス」は設問から削除

## バージョン番号

`script.js`冒頭の`APP_VERSION`定数は画面右上に常時表示される（`package.json`が無い静的アプリのため、ここがバージョンの唯一の出所）。`index.html`/`style.css`/`script.js`に変更を加えてコミットする際は、機能追加か不具合修正かを問わず`APP_VERSION`をパッチ/マイナー単位で上げること。

## 開発メモ由来の背景

個人開発メモの3案のうち、外部データ依存がなく最も着手しやすいと判断してこれを最初に実装した。他の2案:

- インフォグラフィック生成Webアプリ(自伝インフォグラフィック等) — 未着手
- 再販ガンプラチェッカー(次回再販日・キット表示) — 未着手。公式APIが無く、データ源の確保が課題
